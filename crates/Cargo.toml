[workspace]
members = ["cplex-bindings", "compass", "cplex-rs"]

resolver = "2"

[workspace.dependencies]
bindgen = { version = "0.70" }
regex = { version = "1.11" }

# Intra-workspace dependencies
cplex-bindings = { path = "cplex-bindings" }
